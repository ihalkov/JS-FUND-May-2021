// how many exercises
// here in softUni you know that they go from easy to hard
// string
// regex
// objects
// 40 min for one problem, (if not done do next)
// and over 50 do the next
// console.log() to check the tests

function sovle(input) {
    console.log(typeof input);
}

function make(text, argument) {
    return argument === 'Upper'
        ? text.toUpperCase()
        : text.toLowerCase();
}

function printUsername(email) {
    let atIndex = email.indexOf('@');
    if (atIndex > -1) {
        let username = email.substring(0, atIndex);
    }
}

// make your own tests

// let registrations = input; // rename it always
// functions are for helping you if they interrupt you in current moment just don't use at the moment
// easy readable code is needed than performance
// 90/100 and 40 mins passed, go to the next task

// 1:14
